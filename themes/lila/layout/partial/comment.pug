if theme.comment.enable && !page.comment_disable
    if theme.comment.enable == "netease"
        include netease.pug
    else if theme.comment.enable == "disqus"
        include disqus.pug
    else if theme.comment.enable == "facebook"
        include facebook-comments.pug
